services:
  json-server:
    build:
      dockerfile: Dockerfile.json-server
    image: ghcr.io/janko-julianna/joke-jsonserver:0.3

    ports:
      - "3000:80"

  front-end: 
    build:
      dockerfile: Dockerfile.front-end
    image: ghcr.io/janko-julianna/joke-frontend:0.3

    ports:
      - "8001:80"